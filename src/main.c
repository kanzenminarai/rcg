#include <array.h>

int main() {
  // setting ar as a variable from "Array" type
  Array ar;
  
  // initializing the software
  arraySize(&ar);
  arrayType(&ar);
  arrayCases(&ar);
  arrayOutput(&ar);
  return 0;
}
