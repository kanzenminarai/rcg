#include <array.h>

int main() {
  // setting ptr as a variable from "Array" type
  Array ptr;
  
  // initializing the software
  arraySize(&ptr);
  arrayType(&ptr);  
  arrayCases(&ptr);
  arrayOutput(&ptr);
  return 0;
}